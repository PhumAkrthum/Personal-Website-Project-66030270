## วันที่ 1
### 1. โครงสร้าง Semantic HTML
แทนที่จะใช้แท็ก `<div>` ทั่วไปทั้งหน้าเว็บ โครงสร้างเว็บนี้ถูกสร้างขึ้นโดยใช้แท็ก Semantic HTML5 เพื่อเพิ่มความหมายให้กับเอกสารสำหรับเครื่องอ่าน (well-structured data) และเพิ่มความสามารถในการเข้าถึง (Accessibility):
- `<header>`: ใช้สำหรับส่วนหัวกระดาษหลักของเว็บไซต์ที่บรรจุชื่อเว็บ
- `<nav>`: ใช้สำหรับเมนูนำทางหลัก
- `<main>`: ครอบเนื้อหาหลักที่ไม่ซ้ำกันของหน้านั้นๆ
- `<article>` / `<section>`: ใช้สำหรับจัดกลุ่มเนื้อหาหรือหัวข้อต่างๆ ให้เป็นสัดส่วน
- `<aside>`: ใช้สำหรับเนื้อหาหรือลิงก์ที่เกี่ยวข้อง (แถบด้านข้าง)
- `<footer>`: ใช้สำหรับส่วนท้ายของเว็บไซต์

### 2. CSS และข้อควรปฏิบัติ (Best Practices)

*   **External Stylesheet (`<link>`):** เรียกใช้งาน CSS ผ่านแท็ก `<link>` ในส่วน `<head>` ของ HTML (วิธีนี้ให้ประสิทธิภาพดีกว่าและโหลดแบบขนานได้ดีกว่าการใช้ `@import`)
*   **หลีกเลี่ยงการใช้ IDs สำหรับการจัดสไตล์:** เนื่องจากมีคะแนนความเจาะจงที่สูงมาก (Specificity score 1-0-0) ทำให้การเขียนทับ (Override) โค้ดในภายหลังทำได้ยาก

*   **BEM:** นำคลาส (Classes) มาใช้กับทุกส่วน โดยตั้งชื่อตามหลัก **BEM (Block, Element, Modifier)**:
    *   **Block:** `.card`, `.header`, `.nav`
    *   **Element:** `.header__title`, `.nav__list`, `.card__text`
    *   **Modifier:** `.nav__link--active`, `.card--small`
*   **ลดความซ้อนทับ (Flat Specificity):** ไม่เขียน Selector ซ้อนกันเกิน 2 ระดับ เพื่อป้องกันปัญหา "Specificity Wars" และช่วยให้จัดการลำดับขั้น (Cascade) ได้ง่ายขึ้น
*   **CSS Variables (Custom Properties):** ใช้ตัวแปร CSS สำหรับธีมสี เพื่อรักษาความสม่ำเสมอและง่ายต่อการปรับเปลี่ยนธีมในอนาคต

## 🚀 การใช้งานจริง (Deployment)
ฝากเว็บไซต์ไว้ที่ [GitHub Pages](https://PhumAkrthum.github.io/Personal-Website-Project-66030270/).
